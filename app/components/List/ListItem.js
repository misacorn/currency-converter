import React from "react";
import { View, Text, TouchableHighlight } from "react-native";

import styles from "./styles";

const ListItem = ({ text, selected, onPress }) => <TouchableHighlight >

</TouchableHighlight>;

export default ListItem;
